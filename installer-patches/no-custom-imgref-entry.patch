diff --git a/bootc_installer/defaults/image.py b/bootc_installer/defaults/image.py
index f7cb4fca..d7b53ec9 100644
--- a/bootc_installer/defaults/image.py
+++ b/bootc_installer/defaults/image.py
@@ -278,9 +278,6 @@ class BootcDefaultImage(Adw.Bin):
     btn_next        = Gtk.Template.Child()
     search_entry    = Gtk.Template.Child()
     list_images     = Gtk.Template.Child()
-    list_custom     = Gtk.Template.Child()
-    row_custom      = Gtk.Template.Child()
-    image_url_entry = Gtk.Template.Child()
 
     def __init__(self, window, distro_info, key, step, **kwargs):
         super().__init__(**kwargs)
@@ -313,8 +310,6 @@ class BootcDefaultImage(Adw.Bin):
         self.__build_list()
 
         self.search_entry.connect("search-changed", self.__on_search_changed)
-        self.row_custom.connect("notify::expanded", self.__on_custom_toggled)
-        self.image_url_entry.connect("changed", self.__on_url_changed)
         self.btn_next.connect("clicked", self.__on_next_clicked)
 
         self.__select_default()
@@ -466,35 +461,8 @@ class BootcDefaultImage(Adw.Bin):
             else:
                 self.__selected_flatpaks = flatpaks
             logger.info(f"Image selected: {imgref} ({self.__selected_pretty_name})")
-            if self.row_custom.get_expanded():
-                self.row_custom.set_expanded(False)
             self.__update_btn_next()
 
-    def __on_custom_toggled(self, expander, _param):
-        if expander.get_expanded():
-            self.__radio_anchor.set_active(True)
-            self.__selected_imgref = None
-            self.__selected_icon = None
-            self.__selected_carousel = None
-            self.__selected_needs_user_creation = False
-            self.__selected_composefs_backend = False
-            self.__selected_image_type = "bootc"
-            self.__selected_bootloader = ""
-            self.__selected_image_filesystem = ""
-            self.__selected_pretty_name = None
-            self.__selected_default_hostname = ""
-            self.__selected_filesystems = []
-        self.__update_btn_next()
-
-    def __on_url_changed(self, entry):
-        url = entry.get_text().strip()
-        valid = bool(url) and re.match(r"^.+/.+:.+$", url) is not None
-        if url:
-            entry.remove_css_class("error") if valid else entry.add_css_class("error")
-        else:
-            entry.remove_css_class("error")
-        self.__update_btn_next()
-
     # ── Search / filtering ────────────────────────────────────────────────────
 
     def __on_search_changed(self, entry):
@@ -537,12 +505,7 @@ class BootcDefaultImage(Adw.Bin):
     # ── Sensitivity ───────────────────────────────────────────────────────────
 
     def __update_btn_next(self):
-        if self.row_custom.get_expanded():
-            url = self.image_url_entry.get_text().strip()
-            ok = bool(url) and re.match(r"^.+/.+:.+$", url) is not None
-        else:
-            ok = self.__selected_imgref is not None
-        self.btn_next.set_sensitive(ok)
+        self.btn_next.set_sensitive(self.__selected_imgref is not None)
 
     # ── Test / finals ─────────────────────────────────────────────────────────
 
@@ -568,20 +531,6 @@ class BootcDefaultImage(Adw.Bin):
 
     def get_finals(self):
         flatpaks = self.__selected_flatpaks or _FALLBACK_FLATPAKS
-        if self.row_custom.get_expanded():
-            url = self.image_url_entry.get_text().strip()
-            return {
-                "custom_image": url,
-                "pretty_name": _imgref_to_pretty_name(url),
-                "flatpaks": _FALLBACK_FLATPAKS,
-                "carousel": None,
-                "needs_user_creation": False,
-                "composefs_backend": False,
-                "image_type": "bootc",
-                "bootloader": "",
-                "image_filesystem": "",
-                "icon": None,
-            }
         return {
             "selected_image": self.__selected_imgref,
             "pretty_name": self.__selected_pretty_name,
diff --git a/bootc_installer/gtk/default-image.blp b/bootc_installer/gtk/default-image.blp
index 893eef76..532db809 100644
--- a/bootc_installer/gtk/default-image.blp
+++ b/bootc_installer/gtk/default-image.blp
@@ -38,7 +38,7 @@ template $BootcDefaultImage: Adw.Bin {
             margin-top: 24;
             icon-name: "media-optical-symbolic";
             title: _("Choose an Image");
-            subtitle: _("Select a BootcOS image to install, or enter a custom bootc-compatible image URL.");
+            subtitle: _("Select a BootcOS image to install.");
           }
         }
 
@@ -70,24 +70,6 @@ template $BootcDefaultImage: Adw.Bin {
                 ]
               }
             }
-
-            ListBox list_custom {
-              selection-mode: none;
-              hexpand: true;
-
-              styles [
-                "boxed-list",
-              ]
-
-              Adw.ExpanderRow row_custom {
-                title: _("Custom bootc image");
-                subtitle: _("Enter any OCI image reference");
-
-                Adw.EntryRow image_url_entry {
-                  title: _("Image URL");
-                }
-              }
-            }
           }
         }
       }
diff --git a/bootc_installer/utils/processor.py b/bootc_installer/utils/processor.py
index 21b7a634..a302bdd3 100644
--- a/bootc_installer/utils/processor.py
+++ b/bootc_installer/utils/processor.py
@@ -136,10 +136,10 @@ class Processor:
                     encryption_type = "tpm2-luks"
 
         # --- Image / OCI ref ---
-        # In Flatpak mode: finals contain "selected_image" or "custom_image" from the UI.
+        # In Flatpak mode: finals contain "selected_image" from the UI.
         # In live ISO mode: the image step is skipped; recipe["imgref"] holds the remote
         # tracking ref and optional "local_imgref" holds the install source override.
-        image = merged.get("custom_image", "") or merged.get("selected_image", "")
+        image = merged.get("selected_image", "")
         if not image:
             image = sys_recipe.get("imgref", "")
         if not image:
diff --git a/bootc_installer/views/confirm.py b/bootc_installer/views/confirm.py
index 2425ebfd..4a9c9619 100644
--- a/bootc_installer/views/confirm.py
+++ b/bootc_installer/views/confirm.py
@@ -172,14 +172,6 @@ class BootcConfirm(Adw.Bin):
                             "application-x-appliance-symbolic",
                         )
                     )
-                elif key == "custom_image":
-                    self.active_widgets.append(
-                        BootcChoiceEntry(
-                            _("Image"),
-                            value,
-                            "image-missing-symbolic"
-                        )
-                    )
 
         # Hardware-detected GPU badge (always shown if a GPU is found)
         from bootc_installer.core.system import Systeminfo
diff --git a/bootc_installer/windows/main_window.py b/bootc_installer/windows/main_window.py
index 055c1ffe..676baae4 100644
--- a/bootc_installer/windows/main_window.py
+++ b/bootc_installer/windows/main_window.py
@@ -567,7 +567,7 @@ class BootcWindow(Adw.ApplicationWindow):
         image_ref = None
         for f in getattr(self, "finals", []):
             if isinstance(f, dict):
-                image_ref = f.get("custom_image") or f.get("selected_image")
+                image_ref = f.get("selected_image")
                 if image_ref:
                     break
 
diff --git a/tests/ui/test_wizard.py b/tests/ui/test_wizard.py
index 18c0cc82..9a0d936d 100644
--- a/tests/ui/test_wizard.py
+++ b/tests/ui/test_wizard.py
@@ -131,13 +131,13 @@ class TestImageStep:
     def test_get_finals_returns_selected_image(self, window):
         step = window.image_step
         finals = step.get_finals()
-        assert "selected_image" in finals or "custom_image" in finals
+        assert "selected_image" in finals
 
     def test_default_image_selected(self, window):
         """With no default_image in catalog, tree starts collapsed and nothing is pre-selected."""
         step = window.image_step
         finals = step.get_finals()
-        image = finals.get("selected_image") or finals.get("custom_image")
+        image = finals.get("selected_image")
         assert not image, (
             "No image should be pre-selected when default_image is absent from catalog"
         )
@@ -413,10 +413,14 @@ class TestWizardNavigation:
 
 # ── Full flow: finals → processor ─────────────────────────────────────────────
 
-def _set_custom_image(image_step, imgref: str):
-    """Expand the custom image row and type an imgref so get_finals() returns it."""
-    image_step.row_custom.set_expanded(True)
-    image_step.image_url_entry.set_text(imgref)
+def _select_first_catalog_image(image_step):
+    """Activate the first leaf row's check button so get_finals() returns an image.
+
+    Needed because the catalog used in these tests has no default_image, so
+    nothing is pre-selected on load.
+    """
+    _, check, *_rest = image_step._BootcDefaultImage__leaf_rows[0]
+    check.set_active(True)
     _pump()
 
 
@@ -426,7 +430,7 @@ class TestEndToEnd:
         from bootc_installer.utils.processor import Processor
 
         # Select an image so the recipe is valid (no default in catalog).
-        _set_custom_image(window.image_step, "ghcr.io/tuna-os/yellowfin:gnome50")
+        _select_first_catalog_image(window.image_step)
         image_finals = window.image_step.get_finals()
         disk_finals = {
             "disk": {"auto": {"disk": "/dev/vda", "pretty_size": "100 GB",
@@ -454,7 +458,7 @@ class TestEndToEnd:
         from bootc_installer.utils.processor import Processor
 
         # Select an image so the recipe is valid (no default in catalog).
-        _set_custom_image(window.image_step, "ghcr.io/tuna-os/yellowfin:gnome50")
+        _select_first_catalog_image(window.image_step)
         image_finals = window.image_step.get_finals()
         disk_finals = {
             "disk": {
diff --git a/tests/unit/test_confirm_view.py b/tests/unit/test_confirm_view.py
index 8aec683e..7b4ea62e 100644
--- a/tests/unit/test_confirm_view.py
+++ b/tests/unit/test_confirm_view.py
@@ -202,11 +202,6 @@ class TestConfirmUpdate(unittest.TestCase):
         }])
         self.assertGreater(len(obj.active_widgets), 0)
 
-    def test_update_with_custom_image_adds_widget(self):
-        obj = self._make_obj()
-        obj.update([{"custom_image": "ghcr.io/myorg/myimage:latest"}])
-        self.assertGreater(len(obj.active_widgets), 0)
-
     def test_update_sets_btn_confirm_label(self):
         obj = self._make_obj()
         obj.update([])
diff --git a/tests/unit/test_image_helpers.py b/tests/unit/test_image_helpers.py
index b6475c5c..ec78791c 100644
--- a/tests/unit/test_image_helpers.py
+++ b/tests/unit/test_image_helpers.py
@@ -577,8 +577,6 @@ class TestBootcDefaultImagePureLogic(unittest.TestCase):
 
     def _make_widget(self):
         widget = object.__new__(self.cls)
-        widget.row_custom = MagicMock()
-        widget.image_url_entry = MagicMock()
         widget.btn_next = MagicMock()
         widget._BootcDefaultImage__update_btn_next = MagicMock()
         widget._BootcDefaultImage__radio_anchor = MagicMock()
@@ -601,7 +599,6 @@ class TestBootcDefaultImagePureLogic(unittest.TestCase):
         widget = self._make_widget()
         check = MagicMock()
         check.get_active.return_value = True
-        widget.row_custom.get_expanded.return_value = True
 
         with patch.object(
             self.mod,
@@ -631,50 +628,18 @@ class TestBootcDefaultImagePureLogic(unittest.TestCase):
         self.assertEqual(widget._BootcDefaultImage__selected_pretty_name, "New")
         self.assertEqual(widget._BootcDefaultImage__selected_default_hostname, "new-host")
         self.assertEqual(widget._BootcDefaultImage__selected_filesystems, ["btrfs"])
-        widget.row_custom.set_expanded.assert_called_once_with(False)
         widget._BootcDefaultImage__update_btn_next.assert_called_once_with()
 
-    def test_on_custom_toggled_resets_selection_for_custom_image(self):
+    def test_update_btn_next_reflects_selected_imgref(self):
         widget = self._make_widget()
-        expander = MagicMock()
-        expander.get_expanded.return_value = True
-
-        self.cls._BootcDefaultImage__on_custom_toggled(widget, expander, None)
-
-        widget._BootcDefaultImage__radio_anchor.set_active.assert_called_once_with(True)
-        self.assertIsNone(widget._BootcDefaultImage__selected_imgref)
-        self.assertIsNone(widget._BootcDefaultImage__selected_icon)
-        self.assertIsNone(widget._BootcDefaultImage__selected_carousel)
-        self.assertFalse(widget._BootcDefaultImage__selected_needs_user_creation)
-        self.assertFalse(widget._BootcDefaultImage__selected_composefs_backend)
-        self.assertEqual(widget._BootcDefaultImage__selected_image_type, "bootc")
-        self.assertEqual(widget._BootcDefaultImage__selected_bootloader, "")
-        self.assertEqual(widget._BootcDefaultImage__selected_image_filesystem, "")
-        self.assertIsNone(widget._BootcDefaultImage__selected_pretty_name)
-        self.assertEqual(widget._BootcDefaultImage__selected_default_hostname, "")
-        self.assertEqual(widget._BootcDefaultImage__selected_filesystems, [])
-        widget._BootcDefaultImage__update_btn_next.assert_called_once_with()
-
-    def test_update_btn_next_uses_custom_url_validation(self):
-        widget = self._make_widget()
-        widget.row_custom.get_expanded.return_value = True
-        widget.image_url_entry.get_text.return_value = "ghcr.io/example/custom:latest"
 
         self.cls._BootcDefaultImage__update_btn_next(widget)
-
         widget.btn_next.set_sensitive.assert_called_once_with(True)
 
-    def test_on_url_changed_marks_invalid_custom_url(self):
-        widget = self._make_widget()
-        entry = MagicMock()
-        entry.get_text.return_value = "not-a-valid-image"
-        widget._BootcDefaultImage__update_btn_next = MagicMock()
-
-        self.cls._BootcDefaultImage__on_url_changed(widget, entry)
-
-        entry.add_css_class.assert_called_once_with("error")
-        entry.remove_css_class.assert_not_called()
-        widget._BootcDefaultImage__update_btn_next.assert_called_once_with()
+        widget.btn_next.reset_mock()
+        widget._BootcDefaultImage__selected_imgref = None
+        self.cls._BootcDefaultImage__update_btn_next(widget)
+        widget.btn_next.set_sensitive.assert_called_once_with(False)
 
     def test_should_show_skip_screen_and_leaf_count_helpers(self):
         widget = self._make_widget()
@@ -685,7 +650,6 @@ class TestBootcDefaultImagePureLogic(unittest.TestCase):
 
     def test_get_finals_for_selected_catalog_image(self):
         widget = self._make_widget()
-        widget.row_custom.get_expanded.return_value = False
 
         finals = self.cls.get_finals(widget)
 
@@ -703,24 +667,6 @@ class TestBootcDefaultImagePureLogic(unittest.TestCase):
         self.assertEqual(finals["default_hostname"], "example-host")
         self.assertEqual(finals["supported_filesystems"], ["xfs", "btrfs"])
 
-    def test_get_finals_for_custom_image_uses_fallbacks(self):
-        widget = self._make_widget()
-        widget.row_custom.get_expanded.return_value = True
-        widget.image_url_entry.get_text.return_value = "ghcr.io/example/custom-dx:stable"
-
-        finals = self.cls.get_finals(widget)
-
-        self.assertEqual(finals["custom_image"], "ghcr.io/example/custom-dx:stable")
-        self.assertEqual(finals["pretty_name"], "Custom DX")
-        self.assertEqual(finals["flatpaks"], ["org.mozilla.firefox"])
-        self.assertIsNone(finals["carousel"])
-        self.assertFalse(finals["needs_user_creation"])
-        self.assertFalse(finals["composefs_backend"])
-        self.assertEqual(finals["image_type"], "bootc")
-        self.assertEqual(finals["bootloader"], "")
-        self.assertEqual(finals["image_filesystem"], "")
-        self.assertIsNone(finals["icon"])
-
 
 class TestImagesCatalogIntegrity(unittest.TestCase):
     """Regression tests for fisherman/data/images.json content."""
diff --git a/tests/unit/test_processor.py b/tests/unit/test_processor.py
index 8aa47de3..4eabf1ed 100644
--- a/tests/unit/test_processor.py
+++ b/tests/unit/test_processor.py
@@ -412,7 +412,9 @@ class TestMisc:
         assert r["hostname"] == "mergedhost"
         assert r["disk"] == "/dev/vda"
 
-    def test_custom_image_url_overrides_selected(self):
+    def test_stray_custom_image_key_no_longer_overrides_selected(self):
+        """custom_image was removed along with the custom-image UI entry —
+        a stray key (e.g. in an old recipe) must have no effect."""
         finals = [{
             "custom_image": "ghcr.io/custom/image:tag",
             "selected_image": "ghcr.io/x:y",
@@ -422,7 +424,7 @@ class TestMisc:
         }]
         path = Processor.gen_install_recipe("log", finals, _SYS_RECIPE)
         r = _load(path)
-        assert r["image"] == "ghcr.io/custom/image:tag"
+        assert r["image"] == "ghcr.io/x:y"
 
     def test_sys_recipe_fallback_image(self):
         finals = [{"disk": {"auto": {"disk": "/dev/vda"}}, "hostname": "h",
